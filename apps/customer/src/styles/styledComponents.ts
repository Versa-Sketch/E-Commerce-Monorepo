import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  scrollContent: {
    paddingBottom: 140,
  },
  actionContainer: {
    paddingHorizontal: 20,
    marginTop: 20,
  },
  logoutBtn: {
    borderColor: '#E5484D',
    borderRadius: 24,
  },
});
